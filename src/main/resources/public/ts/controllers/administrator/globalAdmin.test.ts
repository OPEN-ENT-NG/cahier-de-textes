import {angular} from "entcore";

describe("test Controller globalAdmin", () => {
    // @Todo try to make unit test for controller
    // var controller, payload = {};
    //
    // angular.mock.module.sharedInjector();
    //
    // beforeAll(() => {
    //     var controllerPayload;
    //     angular.mock.module("app.AppModule");
    //     angular.mock.inject((_$controller_) => {
    //         controllerPayload = _$controller_;
    //     });
    //
    //     controller = controllerPayload("AppModule.globalAdminCtrl");
    //     controller.init(payload);
    // });
    //
    // it("GlobalController init method should be successful", () => {
    //     expect(controller.notebooks).toBeTruthy();
    // });

    it("GlobalController unit test is called but we must fetch our ng-app + module for our controllers", () => {
       expect(true).toBeTruthy();
    });
});