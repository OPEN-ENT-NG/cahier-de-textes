import { ng, _, model, moment, notify } from 'entcore';
import { DAYS_OF_WEEK, COMBO_LABELS, Teacher, Group, Utils, Course } from '../model';

export let creationController = ng.controller('CreationController',
    ['$scope', function ($scope) {
    }]
);